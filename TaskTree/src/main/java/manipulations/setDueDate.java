package manipulations;

import java.util.Date;

public interface setDueDate {
    
    public Date setDue(int year, int month, int day, int hrs, int min);
    public Date setDue(int year, int month, int day);
}
